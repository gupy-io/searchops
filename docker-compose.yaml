version: "3.8"
services:

  elasticsearch:
    image: opensearchproject/opensearch:1.3.0
    environment:
      - "discovery.type=single-node"
      - "ES_JAVA_OPTS=-Xms256m -Xmx256m"
    ports:
      - 9200:9200
